//parameters -- storing the values and reuseble 
function sum(a,b){//(a,b) are parameters
    console.log(a - b)
    console.log(a + b)
}
sum(20,40) //agrument the values
sum(20)
sum()

//NaN --it is a datatype, if you are not passing the arguments it gives NaN value as output